# Add 'feature' label to any PR where the head branch name starts with `feature` or has a `feature` section in the name
enhancement:
  - head-branch: ["^feature", "feature", "^feat", "feat"]

# Add 'bug' label to any PR where the head branch name starts with `bug` or has a `bug` section in the name
bug:
  - head-branch: ["^bug", "bug", "^fix", "fix", "^hotfix", "hotfix"]

maintenance:
  - head-branch: ["^dependabot", "dependabot"]

# Add 'release' label to any PR that is opened against the `main` branch
release:
  - base-branch: "main"

# Add 'pre-release' label to any PR that is opened against the `qa` branch
pre-release:
  - base-branch: "qa"
